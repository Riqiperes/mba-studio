# src/constants

Constantes compartidas entre `apps/web` y `apps/admin` (por ejemplo, los
valores validos de `UserRole`, limites de negocio no configurables). Vacio
por ahora: se llena cuando exista una constante real que ambas apps
necesiten compartir.
