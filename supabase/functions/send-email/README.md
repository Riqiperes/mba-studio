# send-email

Edge Function que envia un correo usando el proveedor de email activo
(`EMAIL_PROVIDER`). Mismo patron de abstraccion que `send-whatsapp`. Ver
`docs/notifications.md`. Sin implementar todavia.
